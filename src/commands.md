# Planned command parity

The bot architecture supports implementing every command in both forms:

- Slash: `/command`
- Prefix: `.command`

The No Prefix system is owner-controlled only. Users granted No Prefix may invoke prefix commands without the configured `.` prefix.

This file is a roadmap; command modules are added incrementally as integrations are implemented and tested.
